exports.moveMembers = function(client, oldChannel, newChannel, delay) {
    //client, message, oldChannel, newChannel
    
};

exports.sleep = function (ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
  }